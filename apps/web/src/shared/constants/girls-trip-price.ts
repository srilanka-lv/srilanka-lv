/** Numeric per-person price in EUR for structured data; keep in sync with the display string below. */
export const GIRLS_TRIP_PRICE_EUR = '1700';

/** The same price as rendered in the trip page hero ribbon. */
export const GIRLS_TRIP_PRICE_DISPLAY = '1.700 EURO';
