{
  "name": "@srilanka/studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --bun sanity dev",
    "build": "bun --bun sanity build",
    "deploy": "bun --bun sanity deploy",
    "schema:extract": "bun --bun sanity schema extract",
    "typegen": "bun --bun sanity schema extract && bun --bun sanity typegen generate"
  },
  "dependencies": {
    "@sanity/vision": "^5.23.0",
    "@srilanka/sanity": "workspace:*",
    "sanity": "^5.23.0",
    "styled-components": "^6.4.1",
    "zod": "^4.4.2"
  }
}
