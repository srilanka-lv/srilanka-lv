# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
pnpm-lock.yaml
package-lock.json
yarn.lock

# testing
coverage

# next.js
.next/
out/

# production
build
dist

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
*-storybook.log
storybook-static

# env files (can opt-in for committing if needed)
.env*
!.env*.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# sanity
.sanity/
schema.json

# claude code / impeccable local state
.claude/settings.local.json
# google search console MCP: service-account key + downloaded binary, never commit
.claude/*.json
!.claude/settings.json
.claude/gsc-mcp-*
# semrush exports (third-party data, keep local)
docs/seo/semrush/
.claude/scheduled_tasks.lock
**/.impeccable/hook.cache.json
**/.impeccable/live/sessions/
